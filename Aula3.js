const notas = [5.5, 6.7, 10];

function media (notas){
    let media = 0;
        for(nta of notas){
            media += nta;

        }
    return media / notas.lengt;
    let medi = media (notas);
    console.log(medi.toFixed(2)); 
}

